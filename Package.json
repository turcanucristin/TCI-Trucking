{
  "name": "tci-trucking",
  "version": "1.0.0",
  "description": "TCI Trucking - Professional Fleet Management",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "init-data": "node -e \"require('node-fetch')('http://localhost:3000/api/init-sample-data', {method: 'POST'}).then(r => r.json()).then(console.log).catch(console.error)\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}